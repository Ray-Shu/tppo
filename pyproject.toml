[project]
name = "tppo"
version = "0.1.0"
dependencies = [
    "ipykernel",
    "jax",
    "flax",
    "distrax",
    "numpy"
]
requires-python = ">=3.11, <3.12"

[tool.uv]
package = false

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"
